import React from 'react';
import styled from 'styled-components';

const HelpModalContent = () => {
    return (
        <Header>Making a Crossword Puzzle</Header>
    );
}

const Header = styled.p`
    font-weight: bold;
    font-size: 24px;
`;

export default HelpModalContent;